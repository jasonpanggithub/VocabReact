# Copy to .env.development or .env.production and adjust.
#
# Development should normally use /api so Vite can proxy to IIS and avoid CORS.
# Production on the IIS vocab2026API virtual app should use /vocab2026API/api.
VITE_API_BASE_URL=/api
